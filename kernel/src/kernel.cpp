// kernel.cpp has been refactored. The kernel entrypoint and initialization
// logic have been moved to `kernel_main.cpp`. Keep this file minimal so the
// build system picks up the new implementation without duplicate symbols.

// Intentionally empty wrapper file.