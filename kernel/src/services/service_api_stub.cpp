#include "services/service_manager.hpp"

namespace kos {
    namespace services {
        namespace ServiceAPI {
            
            /*
            * @brief Starts the manager thread
            * @return True if the thread was started successfully, false otherwise
            */
            bool StartManagerThread() { return false; }
        
        } // namespace ServiceAPI
    } // namespace services
} // namespace kos
