# KOS services configuration
# Lines have format key=value; comments start with '#'
# Enable/disable services by name; names are case-insensitive.
# Keep 8.3 filename for compatibility with our simple FAT reader.

# Global debug logging
# debug=on

debug=off

# Built-in services
service.banner=on
service.time=off
service.fs=on
