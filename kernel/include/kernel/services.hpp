#pragma once
#include <common/types.hpp>

namespace kos { 
    namespace kernel {
        /*
        @brief Register and start all kernel services.
        */
        void RegisterAndStartServices();
    }
}
