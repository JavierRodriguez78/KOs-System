#pragma once

#include "include/common/types.hpp"

namespace kos { 
    namespace net {
        
        /*
        @brief Initializes the RX dispatch system.
        */
        void rx_dispatch_init();

    } 
}